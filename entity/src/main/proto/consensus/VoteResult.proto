syntax = "proto3";
//生成文件所在包名
option java_package = "cn.aberic.bother.entity.proto.consensus";
//生成的java文件名
option java_outer_classname = "VoteResultProto";

import "enums/JoinLevel.proto";

// 加入节点对象
message VoteResult {
    JoinLevel level = 1; // 加入节点所在级别
    repeated string addresses = 2; // 节点地址
}